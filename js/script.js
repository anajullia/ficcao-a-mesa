function corstevenon(){
    $('.quadradoe').css({
        'background-color': '#5c2c49',
        'transform': 'scale(1.2)' // aumenta o tamanho em 20%
    });
}

function corstevenout(){
    $('.quadradoe').css({
        'background-color': '#c86ca3', // define a cor de fundo original
        'transform': 'scale(1.0)' // redefine o tamanho para o normal
    });
}

function coraton() {
    $('.quadradod').css({
        'background-color': '#8b8a23',
        'transform': 'scale(1.2)' // aumenta o tamanho em 20%
    });
}

function coratout() {
    $('.quadradod').css({
        'background-color': '#dad72e',
        'transform': 'scale(1.0)' // aumenta o tamanho em 20%
    });
}
  